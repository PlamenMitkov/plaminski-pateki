/**
 * Sample favorite trail IDs for initial state
 */
export const mockFavoriteIds = [1, 2, 3];

/**
 * Sample auth user context
 */
export const mockAuthUser = {
  userId: 'user-001',
  username: 'plamen',
};

/**
 * Sample sync failure error
 */
export const syncErrorMessage = 'Неуспешна синхронизация на любими.';
